'use strict';

module.exports = function (app) {
    return {
        process: function (next, input, output, args, fileContent, filePath) {
            app.setTemplate(filePath, fileContent || '');
            next();
        }
    };
};
