'use strict';

module.exports = function () {
    return {
        process: function (next, input, output, config, fileContent, filePath) {
            output.set(filePath + '.html', '<script>' + fileContent + '</script>');
            next();
        }
    };
};
